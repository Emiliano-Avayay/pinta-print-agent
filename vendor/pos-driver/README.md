# Driver POS local

Antes de ejecutar `npm run installer:win` en Windows, copiá en esta carpeta el archivo exacto:

`POS Printer Driver Setup V11.3.0.3.exe`

El ejecutable está excluido por Git mediante `.gitignore`. El preflight del instalador verifica su presencia y el Setup final lo incorpora para instalación interactiva.
